# Group integrity and provenance review — 2026-09-13

Reviewed the volume level-2 consumer and GLM provenance at `122fa29cd11e724a6cfc8160841bcfc537bb2c82`, with the frozen `randomise-prep` pin `f740e742411305f72248df5811267671e1a7cc4f`. Scope: discovery → coverage mask → actual helper preparation/generated shell → output publication; lev1 input collection, package identity, residual metadata, and existing completion signatures. Follow-up review includes surface subject identity and RT-arm integrity at discovery. No helper fork or scientific model change is needed.

## Findings and corrections

| Finding | Trigger and control at the reviewed revision | Future corrected behavior |
|---|---|---|
| R1: repeated observations / mixed RT arms | Repeating a root changes two synthetic observations `[3, 20]` into `[3, 3, 20, 20]`. Adding another RT arm also becomes another observation. Disjoint roots with one compatible map per subject behave correctly. | Resolve roots and file aliases; reject repeats, duplicate BIDS `sub-` identities, mislabeled subject directories, incompatible task/contrast names, and mixed RT arms. Errors identify the offending paths. No observation is chosen or discarded to resolve ambiguity. |
| R2: nonfinite effect maps | `NaN != 0` includes the voxel in the group mask and leaves NaN in the real concatenated input. Finite controls work; shifted affines and 4D effect maps already fail. | Read and reject nonfinite consumed maps with source, count, and first voxel index before output/preparation. Preserve geometry checks and the nonzero coverage definition. |
| R3: old output accepted as new success | An exit-zero/no-output executable fails in an empty directory but succeeds with a stale corrected-map filename, even with wrong geometry. A nonzero inferential invocation already fails. | Each attempt gets a fresh sibling directory containing its own mask. Require the expected corrected F map, raw t map, group mask, and correctly sized 4D input; validate readability, geometry and finiteness of these four required products. Optional FSL maps do not gate publication. Stage provenance before publication. |
| D3: stale / incomplete readable provenance | Generator and default tool list name `neuro-workflow`, yielding unknown/not-installed versions. The top-level input list omits consumed fMRIPrep masks and timing sidecars. | Record installed `network_glm` identity/version, fMRIPrep masks, the combined fixed-effects mask, and the timing sidecar used by the runner. Add explicit residual definition, units, scaling, filter cutoffs, detrending/standardization settings, optional FC-confound request, and absence of a censor sample mask. |

The volume `run-manifest.json` now contains an ordered `observations` roster: zero-based `volume_index`, canonical subject, task, contrast, RT arm and input path. It aligns with both the manifest's hashed `inputs` list and `input_data4d.nii.gz`; sorting remains the existing filename order, including across disjoint roots. Existing below-minimum-run selection and upstream provenance-consistency warnings remain.

Surface discovery uses the same subject/task/contrast/RT-arm validation as volume discovery, allowing one map per subject in each hemisphere. It rejects repeated roots (including symlink aliases), repeated files, duplicate subjects within a hemisphere, mislabeled subject directories, and mixed RT arms across either hemisphere. Both CLI and direct surface callers pass through this boundary before loading maps or writing outputs. Disjoint roots remain supported, with the existing sorted order and L/R subject alignment check.

Coverage is **strictly greater than 0.9**, so exactly 9/10 nonzero maps exclude a voxel. Threshold 1.0 retains the existing all-subject intersection special case. Disconnected qualifying voxels remain included (`connected=False`). This corrects the former “>=90%” description without changing the rule.

Successful volume attempts replace the named contrast directory only after validation. Existing results are retained as `.<contrast>.previous-<id>` beside the contrast directory under `--output-dir`; failed attempts remain as `.<contrast>.attempt-<id>` for diagnosis. Retention is a rename of the prior material, not an extra copy, but each successful re-run of a contrast leaves one more superseded directory (including its `input_data4d.nii.gz`) on disk. There is no cleanup policy and no bounded-storage guarantee: operators delete the `.previous-` directories they no longer need. Consumers should use the named contrast directory. An attempt inherits the prior contrast directory's mode before writing any contents and retains it through failure or publication; first runs use the umask-derived mode a plain `mkdir` would give. Failure before publication leaves every earlier output byte intact. The generated script's absolute paths are relocated to the published directory; design/contrast values, `onesample_2sided`, implicit intercept `-1`, seed injection, `--fonly -T -n`, raw `-R` invocation and `bash -e` remain intact. Publication is a directory handoff with rollback on a failed rename, not concurrency coordination for multiple writers of the same contrast.

The expected output names follow the pinned helper's two commands and [FSL's randomise output conventions](https://fsl.fmrib.ox.ac.uk/fsl/docs/statistics/randomise.html). Fake executables validate interface behavior only, not permutation inference, FSL statistical validity or repeatability.

## Provenance boundaries and history

`lev1/cache.py::run_signature` already hashes available run inputs, BOLD timing sidecars, actual package source and numerical dependencies. It is unchanged; these additions extend the readable manifest, not the cache contract or a new hash framework. Input collection retains the existing discovered/attempted-run inventory; completion records identify completed runs. For GIFTI, only the left timing sidecar controls the runner and is added to the readable inventory. A missing sidecar still fails normal run processing. Smoothing mesh identity remains outside this bounded repair.

Residual calculations are unchanged: `Y - X @ beta` using the unwhitened design; volume units are percent signal change with Nilearn's baseline floor of 1, while GIFTI/CIFTI retain input BOLD units. Default temporal filtering remains 0.01–0.1 Hz after GLM extraction, with no detrending/standardization and optional FC confounds. `--no-residual-filter` records null cutoffs; it does not disable separately requested FC-confound regression. `requested` describes the invocation, not proof every discovered run completed.

Volume provenance is staged into the attempt directory before publication only when a real invocation is supplied: `main` always passes its parsed CLI arguments. The volume CLI preserves its documented warn-and-proceed behavior on dirty source: the staged writer permits a truthful `code_dirty: true` stamp even without `--allow-dirty`; that flag silences the warning and remains unchanged in the recorded arguments. The shared provenance primitive and other callers retain their existing dirty-tree guard. A direct library call to `run_level2_analysis` that supplies none gets its validated products with no `run-manifest.json` rather than one describing an invocation, dirty-tree allowance or source dataset that never existed.

Historical outputs and manifests were not rewritten or reinterpreted. This repair describes future corrected runs; absent historical timing/mask identities cannot be recovered from current files. The prior numerical residual correction and its historical implications are documented in [RESIDUALS-REVIEW.md](RESIDUALS-REVIEW.md). No participant incidence of R1–R3 is established by the synthetic reproductions, and no participant, Sherlock or Flywheel computation/mutation occurred. Go/no-go selectors and their tests belong to a separate change. Surface observation integrity is included; its one-sample statistics, permutation defaults, nonfinite-vertex handling, and output publication behavior are unchanged.

## Validation

- Follow-up regressions check restricted attempt permissions before the first mask write, during executable preparation/analysis, and after failure or publication. Generated FSL designs are checked as numeric matrices with declared dimensions; the published script is executed against the synthetic executable and its actual input, mask, design, and output arguments are checked. Surface regressions exercise invalid discovery through the direct API and CLI and compare valid disjoint-root numerical outputs with the same inputs in one root.
- New regression tests were observed failing before their corresponding fixes. They exercise real NIfTI preparation and generated shell calls with a synthetic external executable, duplicate/mixed identities, aliasing, NaN/±Inf, geometry, exact coverage, stale/absent/corrupt products, preserved earlier results, and manifest failure before publication.
- `uv sync --frozen`; `uv run --frozen pytest -q`: **501 passed** at initial implementation, 14 warnings (existing Nilearn mask/deprecation warnings, including the new numerical-control calls), Darwin ARM64 / Python 3.13. No lockfile change.
- `uv build --wheel` succeeds. A separate `uv sync --frozen --no-editable --reinstall-package network-glm` environment imports `network_glm` from its installed `site-packages`, with version **0.1.0**; **23 provenance tests pass** there after review corrections, including mixed-space discovery controls. They cover serialized package version, timing/mask hash changes, preserved completion invalidation, all five CLI spaces' units/filter flags, and real residual/contrast arrays unchanged by metadata writing.
- After review corrections: **516 passed**, 14 existing plotting/Nilearn warnings. Six new cases failed against the preserved pre-fix head and pass after correction: dirty-source publication without the override flag, three optional-map contracts, and corrupt gzip input/output diagnostics. The real dirty-Git test also verifies warning suppression, unchanged invocation arguments, ordered provenance and prior-output retention; required-output rejection and unrelated provenance guards still pass.
- An independent before/after comparison imports the baseline source from `122fa29` and the corrected source into the same frozen environment. With NumPy seed 211, 100 timepoints and 8 synthetic voxels/vertices, volume effects, raw/filtered volume residuals, and raw/filtered surface residuals are array-equal (**maximum absolute difference 0** for each). This is a numerical control for metadata-only changes, not participant validation.
- Native FSL is absent locally. Synthetic executables do not establish FSL numerical validity, participant prevalence, cluster compatibility, or scientific adequacy of the study model. Remote validation/deployment and participant reruns remain separate work.

Reproduce the committed regression coverage with `uv run --frozen pytest tests/lev2 tests/lev1/test_provenance_integrity.py tests/lev1/test_provenance_wiring.py tests/lev1/test_run_cache.py -q`. The fixtures contain only synthetic data.
